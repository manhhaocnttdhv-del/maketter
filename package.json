
{
  "name": "maketter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "dev:backend": "php backend/artisan serve --port=8000",
    "storage:link": "php backend/artisan storage:link"
  }
}
